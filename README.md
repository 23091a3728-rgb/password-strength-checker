A simple Python project to check password strength
To check the strength of a password using security rules.
- Python
- Regular Expressions
1. User enters a password
2. Program checks length, uppercase, lowercase, digits, and symbols
3. Displays password strength
1. Install Python
2. Run the command:
   python password_checker.py
